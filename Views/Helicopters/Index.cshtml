@model IEnumerable<assignment.Models.Helicopter>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Helicopters)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Model_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Purpose)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Helicopters)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Model_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Purpose)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Price }) |
            @Html.ActionLink("Details", "Details", new { id=item.Price }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Price })
        </td>
    </tr>
}

</table>
